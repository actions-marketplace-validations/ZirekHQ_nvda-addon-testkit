name: Notify docs site

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'tests_e2e/**'
      - 'examples/demo-addon/**'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      ref:
        description: Ref or tag to notify about (defaults to the current ref)
        required: false
        type: string

permissions:
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Resolve ref
        id: resolve
        env:
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          INPUT_REF: ${{ inputs.ref }}
          DEFAULT_REF: ${{ github.ref_name }}
        run: |
          if [ "$EVENT_NAME" = "release" ]; then
            ref="$RELEASE_TAG"
          else
            ref="${INPUT_REF:-$DEFAULT_REF}"
          fi
          if ! git check-ref-format --branch "$ref" >/dev/null 2>&1 \
             && ! git check-ref-format "refs/tags/$ref" >/dev/null 2>&1; then
            echo "Invalid ref: $ref" >&2
            exit 1
          fi
          echo "ref=$ref" >> "$GITHUB_OUTPUT"

      - name: Create docs App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ secrets.DOCS_APP_CLIENT_ID }}
          private-key: ${{ secrets.DOCS_APP_PRIVATE_KEY }}
          owner: ZirekHQ
          repositories: ZirekHQ.github.io
          permission-contents: write

      - name: Dispatch update to docs site
        uses: peter-evans/repository-dispatch@28959ce8df70de7be546dd1250a005dd32156697 # v4
        with:
          token: ${{ steps.app-token.outputs.token }}
          repository: ZirekHQ/ZirekHQ.github.io
          event-type: update-docs
          client-payload: |-
            {
              "repository": ${{ toJSON(github.repository) }},
              "ref": ${{ toJSON(steps.resolve.outputs.ref) }},
              "actor": ${{ toJSON(github.actor) }}
            }
