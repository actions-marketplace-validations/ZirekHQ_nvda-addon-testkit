name: Sonar fork scan

# Automated SonarCloud analysis of arbitrary fork PRs doesn't have a safe
# implementation: SONAR_TOKEN is needed to post results, but no matter how
# fork source reaches a job holding SONAR_TOKEN, that job must not execute
# anything derived from the fork. A previous version of this workflow chained
# automatically off sonar-fork-coverage.yml via workflow_run and used
# actions/checkout on the PR head -- actions/checkout itself refuses that
# combination by default ("pwn request" protection), and a purpose-built
# action for this exact problem (matrix-org/sonarcloud-workflow-action) was
# archived with the stated reason: "there is [no] foolproof way to run
# Sonarcloud against attacker-controlled PRs."
#
# So this is comment-triggered: a maintainer reviews the diff, then comments
# `/sonar` on the PR. Only OWNER/MEMBER/COLLABORATOR authors can trigger it
# (checked below), and the job resolves the PR's *current* head itself rather
# than trusting a caller-supplied SHA -- there is no separate "pin the SHA you
# reviewed" input to keep in sync, at the cost of a small window between
# commenting and the job starting in which a new push would get scanned
# instead of what was reviewed.
#
# Unlike a plain checkout-and-build of the fork's code (e.g. a workflow that
# checks out the fork head and runs its own build scripts in a job that holds
# SONAR_TOKEN), this job still never executes anything derived from the fork:
# it downloads the tar archive sonar-fork-coverage.yml built (test suite
# already executed there, safely, with no secrets) and extracts it with plain
# `tar`, never a git operation against fork content. The scanner only reads
# the result statically.
#
# Until a maintainer comments `/sonar`, the required "SonarCloud Code
# Analysis" check on a fork PR stays pending -- merge via admin override if a
# scan isn't warranted (e.g. docs-only changes), rather than faking green.

on:
  issue_comment:
    types: [created]

permissions: {}

concurrency:
  # Every comment on the PR fires this trigger, not just /sonar ones -- so a
  # group keyed on PR number alone would let an unrelated comment (including
  # SonarCloud's own "Quality Gate Passed" bot comment, posted *by* our own
  # run) cancel a scan that's mid-flight. Non-/sonar comments each get their
  # own group, keyed on the comment id, so only genuine /sonar comments ever
  # compete for the slot -- which is what cancel-in-progress is actually for:
  # a second /sonar superseding a stale first one.
  group: sonar-fork-scan-${{ github.event.issue.number }}-${{ startsWith(github.event.comment.body, '/sonar') && 'sonar' || github.event.comment.id }}
  cancel-in-progress: true

jobs:
  acknowledge:
    # The only thing standing between a fork PR and SONAR_TOKEN, so it gates
    # the scan job via `needs` rather than repeating this condition there,
    # where the two copies could drift apart.
    #
    # No reaction/comment back here: if this repo's default GITHUB_TOKEN
    # permissions are read-only (org/repo setting), a job-level
    # `permissions: issues: write` can't actually widen the token -- any
    # write call 403s. Rather than carry a permission this repo may
    # deliberately withhold, this job does nothing but gate.
    if: >-
      github.event.issue.pull_request != null
      && startsWith(github.event.comment.body, '/sonar')
      && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association)
    name: Acknowledge
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      pull-requests: read # needed to tell whether the PR is actually a fork
    outputs:
      is_cross_repository: ${{ steps.pr.outputs.is_cross_repository }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PR_NUMBER: ${{ github.event.issue.number }}
    steps:
      # sonar-fork-coverage.yml only builds its workspace artifact for actual
      # fork PRs -- same-repo PRs are already scanned by sonar.yml -- so a
      # /sonar comment on a same-repo PR must skip `scan` rather than fail on
      # a missing artifact.
      - name: Resolve whether the PR is a fork
        id: pr
        run: |
          gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" \
            --json isCrossRepository \
            --jq '"is_cross_repository=\(.isCrossRepository)"' \
            >> "$GITHUB_OUTPUT"

      - name: Log the trusted trigger
        env:
          COMMENT_USER: ${{ github.event.comment.user.login }}
          COMMENT_ASSOCIATION: ${{ github.event.comment.author_association }}
        run: echo "::notice::/sonar triggered by $COMMENT_USER ($COMMENT_ASSOCIATION)"

  scan:
    name: Sonar fork scan
    needs: acknowledge
    if: needs.acknowledge.outputs.is_cross_repository == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      pull-requests: read # needed by `gh pr view` to resolve the PR's current head
      actions: read # needed to find and download the matching coverage run's artifact
      statuses: write # needed to post the real result to the PR's head commit ourselves (see below)
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PR_NUMBER: ${{ github.event.issue.number }}
    steps:
      - name: Resolve the PR's current head
        id: pr
        run: |
          gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" \
            --json headRefOid,headRefName \
            --jq '"head_sha=\(.headRefOid)\nhead_ref=\(.headRefName)"' \
            >> "$GITHUB_OUTPUT"

      - name: Find the matching coverage run
        id: run
        env:
          HEAD_SHA: ${{ steps.pr.outputs.head_sha }}
        run: |
          RUN_ID=$(gh api "repos/$GITHUB_REPOSITORY/actions/workflows/sonar-fork-coverage.yml/runs?event=pull_request&status=success&head_sha=$HEAD_SHA&per_page=1" \
            --jq '.workflow_runs[0].id')
          if [ -z "$RUN_ID" ] || [ "$RUN_ID" = "null" ]; then
            echo "::error::No successful 'Sonar fork coverage' run found for $HEAD_SHA. Push a commit or re-run that workflow first."
            exit 1
          fi
          echo "id=$RUN_ID" >> "$GITHUB_OUTPUT"

      - name: Download coverage workspace
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: sonar-fork-workspace
          path: ${{ runner.temp }}/sonar-fork-workspace
          run-id: ${{ steps.run.outputs.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract workspace (plain tar, not a git operation)
        env:
          ARCHIVE_DIR: ${{ runner.temp }}/sonar-fork-workspace
        run: |
          mkdir -p workspace
          tar -xzf "$ARCHIVE_DIR/workspace.tar.gz" -C workspace

      # The extracted sonar-project.properties came from the fork. -D args
      # below override its *duplicate* keys, but it could still set things
      # this workflow never pins -- sonar.inclusions, coverage/issue-ignore
      # rules, etc -- to hide code, coverage or findings from the quality
      # gate. Removing it outright is simpler than trying to out-pin every
      # key it might set.
      - name: Remove the fork-controlled Sonar config
        run: rm -f workspace/sonar-project.properties

      # SonarCloud's GitHub integration normally posts the
      # "SonarCloud Code Analysis" status itself, but it attaches to whatever
      # commit the Actions *run* is associated with -- main's tip, since this
      # is triggered by issue_comment, not the PR's head. It has no way to
      # know to target the PR from sonar.pullrequest.key alone. So this waits
      # for the real quality gate result and posts the status to the actual
      # head SHA itself, rather than relying on that automatic posting.
      - name: SonarQube scan
        id: sonar
        if: env.SONAR_TOKEN != ''
        uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f # v8.2.1
        with:
          # The archive's sonar-project.properties came from the fork, so the
          # project identity and scan scope are pinned here explicitly --
          # command-line -D args override the file -- rather than trusted from it.
          args: >
            -Dsonar.projectBaseDir=workspace
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.projectKey=ZirekHQ_nvda-addon-testkit
            -Dsonar.organization=zirekhq
            -Dsonar.sources=src/nvda_testkit,tools
            -Dsonar.tests=tests,tests_spy,tests_e2e
            -Dsonar.exclusions=src/nvda_testkit/_spy/**,tests/fixtures/**
            -Dsonar.python.version=3.13
            -Dsonar.python.coverage.reportPaths=coverage.xml
            -Dsonar.sourceEncoding=UTF-8
            -Dsonar.pullrequest.key=${{ env.PR_NUMBER }}
            -Dsonar.pullrequest.branch=${{ steps.pr.outputs.head_ref }}
            -Dsonar.pullrequest.base=main
            -Dsonar.qualitygate.wait=true
            -Dsonar.qualitygate.timeout=300

      - name: Post the real result to the PR's head commit
        if: always() && env.SONAR_TOKEN != ''
        env:
          SCAN_OUTCOME: ${{ steps.sonar.outcome }}
          HEAD_SHA: ${{ steps.pr.outputs.head_sha }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ "$SCAN_OUTCOME" = "success" ]; then
            STATE=success
            DESC="Quality gate passed"
          else
            STATE=failure
            DESC="Quality gate failed or scan errored -- see the SonarCloud dashboard"
          fi
          gh api --method POST "repos/$GITHUB_REPOSITORY/statuses/$HEAD_SHA" \
            -f state="$STATE" \
            -f context='SonarCloud Code Analysis' \
            -f description="$DESC" \
            -f target_url="https://sonarcloud.io/dashboard?id=ZirekHQ_nvda-addon-testkit&pullRequest=$PR_NUMBER"
